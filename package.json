{
  "name": "homeoffice-server",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^5.0.0",
    "mssql": "^12.0.0",
    "dotenv": "^16.0.0",
    "cors": "^2.8.5"
  }
}